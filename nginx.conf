server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /docs/ {
    try_files $uri $uri/ $uri.html /docs/index.html;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
