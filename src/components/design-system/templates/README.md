# Application layout templates

No standalone shared application template is exported yet. An AppShell template should accept navigation, context/header, content, and feedback slots without coupling to product data or permissions. A gallery template should emerge only after two real pages share its structure and behavior.

Pages and realistic examples remain outside reusable component exports.
