# Contributing

Start with a small component contract: responsibility, props, supported states, keyboard behavior, and a real consumer. Add the catalog example and the focused interaction test in the same change. Keep application-specific requests and workflow rules in the consuming product.

Run `npm run check` before opening a pull request. Do not add customer content, credentials, proprietary fonts, or generated build output.
